
import { projects, technologies, tags } from '../data/projects';

const Projects = () => {

    return (
        <section classname="page">
            <h2>Projects</h2>

            {projects.map((project, ))}
    )
}

